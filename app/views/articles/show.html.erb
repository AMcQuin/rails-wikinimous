<div class="container-md">
  <div class="show-image">
      <h2><%= @article.title%>
        <%= link_to "", edit_article_path(@article), class:"fas fa-feather" %>
        <%= link_to "", article_path(@article), method: :delete, data: { confirm: "Are you sure?" }, class:"fas fa-dumpster-fire" %>
      </h2>
  </div>
      <p> <%= @article.content %></p>
</div>

